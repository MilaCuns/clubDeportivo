<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:orientation="vertical"
    android:background="#FFD733"
    android:padding="24dp"
    android:gravity="center_horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Título -->
    <TextView
        android:text="Socios con cuotas vencidas"
        android:textStyle="bold"
        android:textSize="20sp"
        android:textColor="#000000"
        android:layout_marginBottom="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <!-- Lista de socios -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#FFFFFF"
        android:padding="8dp">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:text="Juan Pérez - 2 cuotas"
                android:textSize="16sp"
                android:padding="8dp"/>
            <TextView
                android:text="Carlos García - 1 cuota"
                android:textSize="16sp"
                android:padding="8dp"/>
            <TextView
                android:text="Marcelo Martínez - 1 cuota"
                android:textSize="16sp"
                android:padding="8dp"/>
            <TextView
                android:text="Cristian Castro - 4 cuotas"
                android:textSize="16sp"
                android:padding="8dp"/>
        </LinearLayout>
    </ScrollView>

    <!-- Botón de pago -->
    <Button
        android:id="@+id/btnPagarCuota"
        android:text="PAGAR CUOTA"
        android:textStyle="bold"
        android:backgroundTint="#000000"
        android:textColor="#FFFFFF"
        android:layout_marginTop="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
</LinearLayout>
